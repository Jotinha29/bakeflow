spring:
  datasource:
    url: jdbc:h2:mem:bakeflow;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ''
  jpa:
    hibernate:
      ddl-auto: validate
  data:
    redis:
      repositories:
        enabled: false
  flyway:
    enabled: true
management:
  health:
    redis:
      enabled: false
