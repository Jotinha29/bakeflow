package com.bakeflow.identity;
import java.time.Instant;import java.util.*;
public final class IdentityDtos{private IdentityDtos(){}public record LoginInput(String email,String password){}public record TokenResponse(String accessToken,UserView user){}public record UserView(UUID id,String name,String email,boolean active,Instant lastLoginAt,Set<String>roles,Set<String>permissions){}public record UserInput(String name,String email,String password,Set<String>roles,boolean active){}public record UserUpdate(String name,String email,Set<String>roles,boolean active){}public record PasswordChange(String currentPassword,String newPassword){}public record SessionView(UUID id,Instant createdAt,Instant lastUsedAt,Instant expiresAt,String userAgent,boolean current){}public record UserPage(List<UserView>content,long total,int page,int size){} }
