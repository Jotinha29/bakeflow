import { ChangeDetectionStrategy, Component, input } from '@angular/core';
@Component({selector:'app-section-card',template:`<section class="bf-section-card">@if(title()){<header><div><h2>{{title()}}</h2>@if(description()){<p>{{description()}}</p>}</div><ng-content select="[sectionActions]" /></header>}<ng-content /></section>`,styles:[`:host{display:block}.bf-section-card{background:var(--bf-surface);border:1px solid var(--bf-border);border-radius:var(--bf-radius-lg);padding:var(--space-6)}header{align-items:center;display:flex;gap:var(--space-4);justify-content:space-between;margin-bottom:var(--space-4)}h2{font-size:1.15rem;margin:0}p{color:var(--bf-text-muted);margin:var(--space-1) 0 0}`],changeDetection:ChangeDetectionStrategy.OnPush})
export class SectionCardComponent {readonly title=input<string>();readonly description=input<string>();}
