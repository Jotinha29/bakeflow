import { ChangeDetectionStrategy, Component, input } from '@angular/core'; import { StatusBadgeComponent, BadgeTone } from '../../../../shared/ui/status-badge/status-badge.component';
@Component({selector:'app-integration-status-card',imports:[StatusBadgeComponent],template:`<article><header><h2>{{name()}}</h2><app-status-badge [label]="statusLabel()" [tone]="tone()"/></header><p>{{description()}}</p>@if(details()){<small>{{details()}}</small>}</article>`,styles:[`article{background:var(--bf-surface);border:1px solid var(--bf-border);border-radius:var(--bf-radius-lg);height:100%;padding:var(--space-6)}header{align-items:center;display:flex;gap:var(--space-3);justify-content:space-between}h2{font-size:1.05rem;margin:0}p{color:var(--bf-text-muted);margin:var(--space-3) 0}small{color:var(--bf-text-muted)}`],changeDetection:ChangeDetectionStrategy.OnPush})
export class IntegrationStatusCardComponent {readonly name=input.required<string>();readonly description=input.required<string>();readonly statusLabel=input.required<string>();readonly tone=input<BadgeTone>('secondary');readonly details=input<string>();}
